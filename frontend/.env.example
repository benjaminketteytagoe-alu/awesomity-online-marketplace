# The frontend talks to the backend through a proxy:
#  - In dev (npm run dev), Vite proxies /api/* to http://localhost:8080
#  - In Docker (production), nginx proxies /api/* to http://backend:8080
#
# Both cases use relative URLs, so this file currently has no
# required variables. This file exists as documentation.
